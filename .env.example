# searxng-mcp — environment config
# Copy to .env and fill in. .env is gitignored.

# Ports (PORT_REGISTRY.md)
LISTEN_PORT=32700
MCP_PORT=33700

# Upstream SearXNG instance
SEARXNG_URL=http://localhost:8888

# Logging
LOG_LEVEL=info
